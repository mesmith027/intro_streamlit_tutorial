import streamlit as st
import pandas as pd
import numpy as np

def advanced(page):
    '''Advanced Streamlit commands'''
    st.title(page)

    st.subheader('Progress and status') 
    # st.progress, st.spinner, error, balloons, warning, info, success, exception 
    
    #with st.spinner('working on it'): 
    #    time.sleep(5)
    #st.success('done!')
    # st.cache 
    # st.stop()
    return
